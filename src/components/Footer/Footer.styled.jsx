import styled from "styled-components";

export const FooterContainer = styled.footer`
  position: absolute;
  bottom: 2.4rem;
  right: 2.4rem;

  display: flex;
  align-items: center;
  gap: 0.4rem;
`;
